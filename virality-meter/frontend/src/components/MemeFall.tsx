'use client';

// Terminal theme - no meme fall effects
interface MemeFallProps {
  isActive: boolean;
  tierLevel: number;
}

export default function MemeFall({ isActive, tierLevel }: MemeFallProps) {
  // Disabled for terminal theme
  return null;
}
